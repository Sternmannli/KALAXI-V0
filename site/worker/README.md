# Cloudflare Worker — Reference Only

This directory contains the AXI Voice Engine designed for Cloudflare Workers edge runtime. It is NOT deployed. The active backend is `public/api/threshold.php` running on HostPoint (PHP 8).

This code is kept as reference for potential future Cloudflare deployment.
